##############################################
# config for customer microservice (db config)
##############################################
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer
    username: amador
    password: amador
    jpa:
      hibernate:
        ddl-auto: create-drop
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true